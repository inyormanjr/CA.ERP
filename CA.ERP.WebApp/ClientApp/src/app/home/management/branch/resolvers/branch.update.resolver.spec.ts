import { Branch.Update.Resolver } from './branch.update.resolver';

describe('Branch.Update.Resolver', () => {
  it('should create an instance', () => {
    expect(new Branch.Update.Resolver()).toBeTruthy();
  });
});
