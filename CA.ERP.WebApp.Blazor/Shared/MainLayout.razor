@inherits LayoutComponentBase



<MudThemeProvider Theme="@Theme" />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar Elevation="1" Class=" justify-center">
        <MudText Typo="Typo.h6" Color="Color.Dark">Citi Appliance</MudText>
        <MudAppBarSpacer />
        <LoginDisplay />

    </MudAppBar>

    <MudMainContent>
        <MudContainer Class="d-flex justify-center py-2 px-1 mt-6">
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4" Link="/authent">Primary</MudButton>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4">Primary</MudButton>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4">Primary</MudButton>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4">Primary</MudButton>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4">Primary</MudButton>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4">Primary</MudButton>
                <MudButton Variant="Variant.Text" Color="Color.Primary" Class="pa-4">Primary</MudButton>

        </MudContainer>
        @Body
    </MudMainContent>
</MudLayout>

@code{
    MudTheme Theme = new MudTheme()
    {
        Palette = new Palette()
        {

            AppbarBackground = Colors.Shades.White,
        },


    };
}
